/**
 * @crownx-jewel/shared-xp — the canonical /LV99 status economy.
 * Shared by the client (status page, badges) and the xp-service so rank is
 * computed identically everywhere.
 */
export * from "./economy";

export type AppRole = "fan" | "creator" | "authenticator" | "admin";

export const Roles = {
  Fan: "fan" as AppRole,
  Creator: "creator" as AppRole,
  Authenticator: "authenticator" as AppRole,
  Admin: "admin" as AppRole
};

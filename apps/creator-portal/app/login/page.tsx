export default function LoginPage() {
  return (
    <main style={{ padding: 24, maxWidth: 480, margin: "0 auto" }}>
      <h1>Sign in</h1>
      <p style={{ color: "#475467" }}>
        Wave 1 dev mode: auth is stubbed. Use the API directly or the forms in
        the Assets and Listings pages. Wave 2 wires in real JWT login.
      </p>
    </main>
  );
}
